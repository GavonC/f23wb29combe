extends layout

block content
  h1= title
  p  #{title}
  - const results=[];
  <div class="container-fluid">
    <div class="row"> 
      <div class="col-sm-2">
      - results ${hammer}
      - results ${large}
      - results ${25.99}
      </div>
    </div>
  </div>

